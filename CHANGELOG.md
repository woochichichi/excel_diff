# 변경 이력 (ExcelDiffMerge)

버전관리는 유의적 버전(Semantic Versioning)을 따른다: MAJOR.MINOR.PATCH.
빌드 산출물의 어셈블리 버전(`src/AssemblyInfo.cs`)과 `VERSION` 파일을 릴리스마다 함께 갱신한다.

---

## [1.10.0] - 2026-07-16

N-way 취합 화면 전면 재설계 — 메인 화면과 같은 좌우 비교 방식으로.

### 변경 (Changed)
- **N-way 취합을 좌우 비교로 재설계**(요청): 기존 "모든 차이를 한 표에 나열" 방식
  폐기. 왼쪽=base 고정, 오른쪽=선택한 버전 1개(상단 콤보 "v1: 파일명 (변경 N)" 로
  전환) — 익숙한 2-way 화면 그대로 버전을 하나씩 검토.
  - 채택: 오른쪽 차이 셀 **더블클릭** 또는 가운데 **[←]** 버튼 → base 에 채택 대기
    (base 셀 파랑 표시, 값 즉시 반영 표시). 버전을 전환해도 채택 누적 유지,
    다른 버전 값으로 재채택 시 교체 확인.
  - **충돌 표시**: 여러 버전이 같은 셀을 다르게 변경하면 주황 + 툴팁으로 어느
    버전들이 충돌하는지 안내.
  - **[base 에 저장 (N)]** 버튼을 base 그리드 아래 배치(메인 화면과 동일 동선),
    Ctrl+Z 채택 취소, F7/F8 차이 이동, 동기 스크롤/마커바/가상 그리드(대용량).
  - 비교 후 파일 선택 패널 자동 접힘(툴바에서 다시 펼치기).

## [1.9.0] - 2026-07-16

조작 동선 전면 개편(현장 피드백): 자동 비교 + 화면 안 병합/저장.

### 변경 (Changed)
- **자동 비교**: 좌/우 파일이 모두 로드되면 [비교]를 누를 필요 없이 즉시 비교.
  드래그앤드롭도 동일 — 2개 동시 드롭, 1개 드롭으로 쌍 완성, 비교 후 한쪽 교체
  모두 자동 (재)비교. 툴바 버튼은 [다시 비교]로 유지.
- **병합 버튼을 두 그리드 사이로**: 주 툴바의 병합 버튼을 없애고, 그리드 사이
  중앙 세로 스트립에 큰 화살표 [→]/[←] 배치 — 값이 가는 방향이 화면 배치
  그대로 읽힘. Alt+→/← 단축키 유지.
- **[이 파일에 저장] 버튼을 각 그리드 우측 하단으로** 이동 — "이 화면의 파일에
  저장"이 직관적으로 보이게.
- **주 툴바 단순화**: [좌측 열기][우측 열기] | [이전차이][다음차이] | [다시 비교]
  | [저장(양쪽)] — 사용 순서(왼쪽→오른쪽)대로만. 온보딩 안내 문구 갱신.

### 추가 (Added)
- **셀 더블클릭 = 반대쪽 반영**: 차이 셀을 더블클릭하면 그 값이 반대쪽 병합
  대기로 등록.
- **셀 드래그 병합**: 선택 셀(들)을 반대쪽 그리드로 끌어다 놓으면 병합 대기 등록.
  (파일 드래그 열기와 자동 구분)
- **Ctrl+Z 되돌리기**: 병합 등록 동작(화살표/더블클릭/드래그/전체병합/개별취소)을
  동작 단위로 취소.

## [1.8.0] - 2026-07-16

현장 피드백 반영: 그리드 속도 + 저장 직관화 + 툴바 정리.

### 변경 (Changed)
- **그리드 표시 속도 대폭 개선**: DataGridView 더블버퍼링 적용(스크롤/리페인트
  지연의 주범), 셀 페인트 경로의 문자열 키 생성 제거(GC 압박 제거), 단일 셀
  클릭은 빠른 경로로 분리, 같은 셀 중복 상세갱신 생략.
- **파일별 [이 파일에 저장] 버튼**: 각 그리드 위 파일 경로 옆에 배치 — 어느
  파일이 덮어써지는지 명확. 해당 방향 병합 대기 건수를 버튼에 표시하고 대기가
  있을 때만 활성. 툴바 [저장]은 양쪽 저장(툴팁 명시).
- **보조 툴바 그룹화**: 정렬 / 보기 / 병합 / 도구 그룹 캡션+구분선으로 정리.
  위험 동작(전체병합 2종·병합 취소)은 연주황 배경으로 구분.
- **색상 범례를 우측 하단(상태바)으로 이동** — 상단 공간 회수(그리드 확대).

### 수정 (Fixed)
- 한쪽만 저장 후 자동 재비교 시 저장하지 않은 반대쪽 병합 대기가 유실되던
  문제(파일별 저장 도입 과정에서 발견) — 재비교 후 복원.

## [1.7.0] - 2026-07-16

사용자 편의성/GUI 대폭 개선 + 버그 7건 수정.

### 수정 (Fixed)
- **다중 셀 병합이 반대 방향에서 1개만 적용되던 버그**: 선택 동기화가 반대편 그리드의
  다중 선택을 1칸으로 접어, 드래그 선택 후 반대 방향 병합 시 셀 1개만 병합되던 문제.
  선택 전체를 미러링하고, 병합 시 더 많이 선택된 그리드를 기준으로 좌표를 잡도록 수정.
- **전체 병합이 반대편 신규 행 데이터를 조용히 지우던 버그**: 소스에 없는 행(추가/삭제)의
  셀이 빈값으로 병합 등록되던 문제 — 이제 "대응 불가" 카운트로 제외.
- **로드/비교 중 창을 닫으면 앱 전체가 죽던 크래시**: STA 워커의 완료 콜백 예외를
  로깅 후 무시하도록 보호.
- **N-way: base 에 없는 시트의 값을 채택하면 저장 전체가 실패하던 버그**: 채택 단계에서
  차단 + 저장 시 없는 시트는 건너뛰고 개수 안내(정상 셀 저장은 항상 성공).
- **N-way 가 Excel 리더 하나를 재사용하던 문제**: 파일마다 리더 생성/해제로 변경
  (연속 로드 시 간헐 COM 오류 회피 — 2-way 와 동일 패턴).
- **숫자↔문자 타입 변경 미감지**: 숫자 100 → 텍스트 "100" 변경이 '동일'로 표시되던
  문제. 한쪽만 숫자 타입이면 '변경'으로 감지(+셀프테스트 회귀 케이스 추가).
- **'키 컬럼' 정렬에서 키열이 비면 조용히 LCS 로 폴백하던 문제**: 비교 전에 경고하고
  사용자가 선택(LCS 진행/중단), 상태바에 실제 적용 모드 표시.

### 추가 (Added) — 사용자 편의성
- **[최근 비교] 드롭다운**(보조 툴바): 이전에 비교한 좌/우 파일 쌍을 골라 바로 재비교.
- **단축키 확충**: F5=비교, Ctrl+Shift+O=우측 열기, Alt+→/←=병합, Ctrl+F=변경목록 검색.
  버튼 툴팁에 단축키 병기.
- **변경 목록 검색/필터**: 검색어(시트/위치/값 부분일치) + 종류(변경/추가/삭제) 필터.
- **값 복사 지원**: 하단 셀 상세를 복사 가능한 컨트롤로 교체, 그리드 Ctrl+C 복사 허용.
- **셀 내 변경 구간 하이라이트**: 상세 패널에서 좌/우 값의 달라진 부분만 색으로 강조.
- **그리드 글자 크기 조절**: Ctrl+마우스휠 또는 [글자+]/[글자−], 크기는 settings.ini 에 기억.
- **시트 간 차이 이동**: F7/F8 이 현재 시트 끝에 닿으면 변경 있는 다음/이전 시트로 자동
  전환. 상태바에 전역 진행도("차이 X/Y — 시트 A/B") 표시.
- **병합 취소**: [병합 취소] 버튼(전체 초기화) + 셀 우클릭 "이 셀 병합 취소".
- **[열 자동맞춤]** 버튼 + 열 헤더 더블클릭으로 열 너비 내용 맞춤(양쪽 동시).
- **COM 열기 실패 원인 안내**: HRESULT 를 한국어 문구로 매핑(권한 없음/파일 열려 있음/
  COM 연결 끊김)해 오류 메시지 첫 줄에 표시.

## [1.6.1] - 2026-07-16

Windows/COM 하드닝.

### 수정·강화 (Hardened)
- **좀비 프로세스 방지 최후 안전망**(`ComProcessGuard`): 우리가 만든 Excel/Word 인스턴스의
  PID 를 Application.Hwnd 로 확보해 두고, Quit+Release+GC 후에도 살아있으면 **그 PID 만**
  강제 종료. 프로세스 이름(EXCEL/WINWORD) 일치 시에만 종료해 사용자가 열어둔 Office 는
  건드리지 않음. (Excel 지원, Word 는 Hwnd 미제공이라 Quit+GC 로 처리)
- **COM 누수 감소**: `UsedRange.Rows/.Columns` 를 지역변수로 받아 즉시 해제(two-dot 체인 회피).
- **CSV 인코딩 자동 판별**(한국 Windows): BOM(UTF-8/UTF-16) 우선, 없으면 UTF-8 유효성 검사 후
  실패 시 시스템 기본(CP949)로 읽음. 엑셀 저장 한글 CSV(CP949) 가 깨지지 않도록.

## [1.6.0] - 2026-07-16

전체 병합 + 버튼 라벨 최종 정리.

### 추가 (Added)
- **전체 병합**(보조 툴바): [모두 왼쪽>오른쪽] / [모두 왼쪽<오른쪽] — 모든 시트의
  모든 차이 셀을 한쪽 값으로 한꺼번에 병합 대기 등록(확인 후, [저장]으로 반영).
  행 추가/삭제로 대응 불가한 셀은 제외하고 개수 안내.

### 변경 (Changed)
- 차이 이동 버튼에서 화살표 제거: "이전차이" / "다음차이"(텍스트만).
- 병합 버튼을 부등호로: **"왼쪽 > 오른쪽"**(왼쪽값을 오른쪽으로) / **"왼쪽 < 오른쪽"**
  (오른쪽값을 왼쪽으로) — 부등호가 값이 가는 방향(목적지)을 가리킴.

## [1.5.0] - 2026-07-16

Word(.docx) 비교 지원 + 병합 버튼 화살표 + N-way 버튼 가시성.

### 추가 (Added)
- **Word 문서(.doc/.docx/.docm) 비교 지원** (`WordComReader`, Word COM late binding).
  문서를 "1열짜리 시트"(각 문단 = 한 행)로 표현 → 기존 diff/정렬/그리드/변경목록을
  그대로 재사용해 **문단 단위 비교**. DRM 은 Word 가 복호화(직접 파싱 없음).
  본문은 `Content.Text` 로 한 번에 읽음. [열기] 필터에 Word 추가, 단일열은 넓게 표시.
  ※ 1차는 비교/보기 전용(Word 병합 저장 미지원 — 시도 시 안내). CSV/Word 세션은 병합 버튼 비활성.

### 변경 (Changed)
- **병합 버튼을 화살표로**(요청): "왼쪽값 → 오른쪽" / "왼쪽 ← 오른쪽값".
  값이 흐르는 방향(→ 는 목적지)으로 직관화. 차이이동은 삼각형(◀▶)이라 글리프로 구분.
- **N-way 창 버튼 가시성 개선**: 버튼을 크게+배경색+번호(①②③④⑤)로 '버튼'임을 명확히,
  전문 렌더러 적용. (기존엔 텍스트처럼 보여 클릭 가능한지 몰랐던 문제)

## [1.4.0] - 2026-07-16

현장 사용성 피드백 반영(변경 찾기/버튼 혼동/저장 흐름/도움말).

### 추가 (Added)
- **변경 목록 패널(우측)**: 모든 차이(시트·위치·종류·좌→우 값)를 목록으로 표시.
  항목을 클릭하면 해당 셀로 바로 이동 → 일일이 스크롤로 찾을 필요 없음.
  보조 툴바 [변경목록] 으로 표시/숨김. 변경 마커바도 넓혀(20px) 가시성 개선.
- **N-way 취합 사용법 안내**: 빈 화면에 단계별 설명 표시(비교 시 숨김).
- **CSV 폴더 비교 사용법 안내**: 실행 전 용도/방법 설명 다이얼로그.

### 변경 (Changed)
- **병합 버튼 라벨 변경**: 차이 이동 화살표(◀▶)와 헷갈리지 않도록 병합 버튼은
  화살표 대신 말로 — "왼쪽값을 오른쪽에" / "오른쪽값을 왼쪽에". 이전/다음은 화살표 유지.
- **저장 흐름 개선**:
  - 저장 전 **어느 파일을 덮어쓰는지(전체 경로) + 백업 여부**를 명확히 확인
    (예/아니오/취소). 원본이 Excel에 열려 있으면 닫으라는 안내 포함.
  - 저장 후 **저장 위치·백업 경로**를 안내.
  - 저장 후 **디스크에서 자동 재로드 + 재비교** → 화면이 저장 결과로 갱신
    (기존: 저장해도 화면이 안 바뀌던 문제 해결).
- 읽기전용 저장 실패 메시지에 "원본을 Excel에서 닫고 다시" 안내 추가.

### 확인 (Verified — 현장 로그)
- 소프트캠프 DRM 파일에 **COM 병합 저장이 정상 동작**함을 현장 로그로 확인(§11 해소).
  읽기전용 실패는 원본을 Excel에서 열어둔 경우였음.

## [1.3.0] - 2026-07-16

현장 로그로 발견한 치명적 버그 수정 + 파일 열기 즉시 미리보기.

### 수정 (Fixed) — 중요
- **COM 로드 실패(RPC_E_INVALID_OBJECT, 0x80010114) 크래시 수정.**
  `Release(ref dynamic o)` 의 `o == null`(동적 동등연산)이 이미 끊긴 COM 객체에서
  DLR 바인더의 QueryInterface 를 유발해 예외를 던졌다. 명시적 `(object)` 참조변환으로
  dynamic 을 벗겨낸 뒤 정적 코드로만 해제하도록 변경(ExcelComReader/MergeEngine 동일).
  Dispose 의 `_xl != null` 동적 비교도 같은 방식으로 수정.
- 비교 시 두 워크북을 **각자의 리더(Excel 인스턴스)로 따로 열고 닫도록** 변경 →
  한 세션에서 연달아 여닫을 때 생기던 COM 상태 문제를 근본 회피.

### 추가 (Added) — UX
- **파일 열기 즉시 미리보기**: [좌측/우측 열기] 하면 그 파일 내용을 해당 그리드에
  바로 표시(색상 없는 원본 미리보기). 단일 파일 드래그앤드롭도 미리보기.
- **캐시 기반 비교**: 이미 열어 둔(미리보기) 파일은 [비교] 시 재열기 없이 즉시 비교.
- 미리보기 상태에서는 네비/병합/변경만 버튼을 비활성(진짜 비교 후에만 활성).

## [1.2.0] - 2026-07-16

로깅 강화(폐쇄망 진단) + GUI/UX 개편.

### 추가 (Added)
- **파일 로거(`Logger`)**: exe 옆 `ExcelDiffMerge.log`(쓰기 불가 시 %TEMP%)에
  동작·오류를 기록. COM 오류는 **HRESULT 까지** 남겨 DRM/열기 예외 진단(§11)에 활용.
  2MB 초과 시 `.log.1` 로 롤오버. 스레드 안전.
- **전역 예외 로깅**: UI 스레드 예외(`Application.ThreadException`) +
  미처리 예외(`AppDomain.UnhandledException`) 를 로그로 남기고 사용자에게 로그 경로 안내.
- 주요 지점 로깅: 앱 시작/종료·환경, COM 인스턴스 생성/정리, 파일 Open(성공/실패),
  로드 완료(시트 수), 비교 결과 요약, 병합 대기/저장(성공/실패).
- 보조 툴바에 **[로그 열기]**(로그 파일 위치 안내) 추가.
- 창 크기/최대화 상태 기억·복원(settings.ini).

### 변경 (Changed) — GUI/UX
- **툴바를 2단으로 분리(위계)**: 자주 쓰는 동작은 **큰 주 툴바**(좌/우 열기, 비교,
  이전/다음, 좌→우/우→좌, 저장 — '비교'·'저장'은 굵게), 옵션은 **작은 보조 툴바**
  (정렬/키열, 변경만 보기, N-way, CSV폴더비교, 로그 열기).
- **버튼 활성/비활성 상태화**: 비교 전에는 네비/병합/저장 비활성, 저장은 병합 대기가
  있을 때만 활성 — 오조작 감소.
- 모든 버튼에 **툴팁** 추가.

## [1.1.0] - 2026-07-15

로컬 테스트 가능화 + 행 정렬(cascade 방지) + UX 강화 + 시장/오픈소스 조사 반영.

### 추가 (Added)
- **행 정렬 엔진(`RowAligner`)** — 행 삽입/삭제로 하위 행이 전부 "변경"으로 오판되는
  cascade 문제 해결(spec §5-4). 3가지 모드:
  - `자동정렬(LCS)`: 유사도 기반 LCS. "채워진 셀의 과반 일치"를 앵커로 삼아
    삽입·삭제를 인지하면서 수정된 행은 delete+insert 가 아닌 '변경'으로 짝지음.
  - `키 컬럼`: 키 값 기준 조인(O(n), 대용량/재정렬에 강함).
  - `좌표`: 기존 방식(가장 빠름, 삽입/삭제 미인지).
- **로컬 테스트 인프라** (Excel/COM/DRM 불필요):
  - `CsvWorkbookReader`: CSV 폴더(각 .csv=시트)를 동일 `WorkbookData` 로 로드.
  - GUI `[CSV폴더비교]`, CLI `--difcsv <좌폴더> <우폴더>`.
  - CLI `--selftest`: 메모리 목업으로 DiffEngine/RowAligner 자기검증(현장에서 즉시 확인).
  - `tests/reference.py`: 파이썬 가상테스트(알고리즘 레퍼런스, 리눅스에서도 실행 가능).
  - `tests/data/` 목업 CSV(변경/추가/삭제/행삽입/행삭제/수식/시트추가·삭제 케이스).
  - `tests/make_xlsx.py` + `tests/data/old.xlsx`,`new.xlsx`: 외부 라이브러리 0 으로
    생성한 실제 .xlsx 목업(진짜 수식 포함) → Excel COM 경로/DRM 흐름 로컬 테스트.
- **UX (시장조사 반영)**:
  - 셀 상세 패널(하단): 선택 셀의 좌/우 값·수식·상태 표시.
  - 색상 범례 바(변경/추가/삭제/병합됨/정렬빈칸).
  - 변경 마커바(우측 스크롤): 차이 밀도 시각화 + 클릭 점프.
  - 정렬 방식 선택 콤보 + 키 컬럼 입력.
  - 단축키: F7/F8(다음/이전 차이), Ctrl+S(저장), Ctrl+O(열기).
  - 시트 탭에 변경 건수/추가·삭제 표기.

### 변경 (Changed)
- **COM 을 STA 전용 스레드에서 실행**(`StaTask`) — 기존 `BackgroundWorker`(MTA)는
  간헐적 RPC/마샬링 오류 위험(조사 반영). 로드/저장을 STA 스레드로 이동, UI 는 Invoke 마샬링.
- **diff 모델을 정렬 기반으로 재설계** — `SheetDiff` 가 좌표 셀 리스트 대신
  정렬된 표시행(`DiffRow`, 좌/우 행 개별) 을 보유. 좌우 그리드가 논리적으로 같은
  레코드를 같은 화면행에 표시.
- `Workbooks.Open` 파라미터 강화: `IgnoreReadOnlyRecommended=true`, `Notify=false`
  로 '이미 열림/읽기전용 권장' 프롬프트 선제 차단.
- `AutomationSecurity` 는 프로세스 전역이므로 종료 시 원복.
- `Interactive=false` 추가.

### 문서 (Docs)
- README 에 테스트/정렬모드/버전 안내 추가.

---

## [1.0.0] - 2026-07-15

초기 구현(C# WinForms + Excel COM late binding).

### 추가
- Excel COM 리더(UsedRange 일괄 로드, 역순 ReleaseComObject, 좀비 방지).
- 2-way diff(시트 매칭, 표시값+수식 비교), N-way 취합(충돌 감지).
- 좌우 병렬 그리드(가상모드), 색상, 동기 스크롤, 시트 탭, F7/F8 네비, 변경만 보기.
- 방향 병합/저장(백업), settings.ini, 드래그앤드롭, 온보딩.
- 현장 csc 빌드(`build.bat`, winexe/x64), README.
